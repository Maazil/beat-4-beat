name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v5

      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Create .env file
        run: |
          echo "VITE_FIREBASE_API_KEY=${{ secrets.VITE_FIREBASE_API_KEY }}" >> .env
          echo "VITE_FIREBASE_AUTH_DOMAIN=${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}" >> .env
          echo "VITE_FIREBASE_PROJECT_ID=${{ secrets.VITE_FIREBASE_PROJECT_ID }}" >> .env
          echo "VITE_FIREBASE_STORAGE_BUCKET=${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}" >> .env
          echo "VITE_FIREBASE_MESSAGING_SENDER_ID=${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}" >> .env
          echo "VITE_FIREBASE_APP_ID=${{ secrets.VITE_FIREBASE_APP_ID }}" >> .env
          echo "VITE_FIREBASE_MEASUREMENT_ID=${{ secrets.VITE_FIREBASE_MEASUREMENT_ID }}" >> .env
          echo "VITE_SPOTIFY_CLIENT_ID=${{ secrets.VITE_SPOTIFY_CLIENT_ID }}" >> .env
          echo "VITE_SPOTIFY_REDIRECT_URI=${{ secrets.VITE_SPOTIFY_REDIRECT_URI }}" >> .env

      - run: pnpm build

      # Deploy Firestore rules before hosting so new rules are live before
      # the frontend that depends on them.
      - name: Deploy Firestore rules
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BEAT_4_BEAT }}
          GOOGLE_APPLICATION_CREDENTIALS: ${{ runner.temp }}/firebase-sa.json
          # Runners hit "Premature close" on googleapis.com's OAuth token
          # endpoint via Node's IPv6 Happy-Eyeballs path; prefer IPv4.
          NODE_OPTIONS: --dns-result-order=ipv4first
        run: |
          # The secret may be raw JSON or base64-encoded. FirebaseExtended's
          # hosting action (below) accepts both; mirror that here so the rules
          # deploy authenticates with the same credentials.
          if printf '%s' "$FIREBASE_SERVICE_ACCOUNT" | head -c 1 | grep -q '{'; then
            printf '%s' "$FIREBASE_SERVICE_ACCOUNT" > "$GOOGLE_APPLICATION_CREDENTIALS"
          else
            printf '%s' "$FIREBASE_SERVICE_ACCOUNT" | base64 -d > "$GOOGLE_APPLICATION_CREDENTIALS"
          fi
          # Fail early with a clear message if the credentials aren't valid JSON.
          node -e "JSON.parse(require('fs').readFileSync(process.env.GOOGLE_APPLICATION_CREDENTIALS,'utf8'))" \
            || { echo "::error::FIREBASE_SERVICE_ACCOUNT did not decode to valid JSON"; exit 1; }
          # --debug surfaces the real auth error; requireAuth otherwise masks
          # causes like "invalid_grant" behind "have you run firebase login?".
          # Retry to ride out transient "Premature close" token-fetch drops.
          ok=0
          for attempt in 1 2 3; do
            if npx firebase-tools@14 deploy --only firestore:rules --project beat-4-beat --debug; then
              ok=1; break
            fi
            echo "rules deploy attempt $attempt failed; retrying in 5s"
            sleep 5
          done
          rm -f "$GOOGLE_APPLICATION_CREDENTIALS"
          [ "$ok" = 1 ] || { echo "::error::firestore rules deploy failed after 3 attempts"; exit 1; }

      - if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BEAT_4_BEAT }}
          channelId: live
          projectId: beat-4-beat
